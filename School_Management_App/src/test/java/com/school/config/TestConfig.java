package com.school.config;

import org.springframework.boot.test.context.TestConfiguration;

@TestConfiguration
public class TestConfig {
    // This class can be used for any custom test configurations if needed in the future
}
